#include <stdio.h>
int main(){
    int x=0,testes[100],f=0;
    int m[100][100];
    while (x!=0)
    {
        scanf("%d",&x);
        testes[f]=x;
        f++;
    }
    for (int i = 0; i < f; i++)
    {
        m[testes[i]][testes[i]];
        if (testes[i]%2==0)
        {
            /* code */
        }
        
    }
    
    

    return 0;
}