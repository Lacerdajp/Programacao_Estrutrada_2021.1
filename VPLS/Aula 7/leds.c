#include <stdio.h>
#include <string.h>
int main()
{
    int n[1000], leds = 0, count[30];
    int v = 0, result = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &v);
        n[i] = v;
        result = v;
        while (result != 0)
        {
        }
    }

    return 0;
}